# 架构决策记录 0003：使用 AGPL-3.0-or-later

状态：已接受。

## 背景

项目希望作为自由软件发布，允许商业使用，同时确保对工具本身的改进在分发修改版或作为网络服务提供时继续开放源码。

## 决策

`ProleCoder` 使用 `AGPL-3.0-or-later` 授权。

## 影响

- 允许商业使用。
- 分发修改版时必须提供对应源码。
- 修改版作为网络服务提供给用户时，也必须向这些用户提供对应源码。
- 贡献者提交的贡献按 `AGPL-3.0-or-later` 授权。
